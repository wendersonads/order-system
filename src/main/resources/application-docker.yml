spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/orderdb
    username: admin
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
